// Represent a single value
// integer, floats, boolean, character
// Rust is statically types, but this version uses type inference

fn main() {
    let a: i32 = 10; // integer
    let b: f64 = 20.5; // float
    let c: bool = true; // boolean
    let d: char = 'R'; // character
}